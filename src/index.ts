import { default as Button } from '@/components/buttons/Button.vue';
import { setupTheme } from '@/setup-theme';
import type { ThemeConfig, ThemeContent } from '@/composables/theme';

export { Button, ThemeConfig, ThemeContent, setupTheme };
